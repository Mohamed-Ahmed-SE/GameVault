/**
 * GameVault Theme Barrel Export
 */

import { colors } from './colors';
import { typography } from './typography';
import { spacing, borderRadius, cardDimensions } from './spacing';
import { shadows } from './shadows';

export const theme = {
  colors,
  typography,
  spacing,
  borderRadius,
  cardDimensions,
  shadows,
};

export { colors, typography, spacing, borderRadius, cardDimensions, shadows };
export default theme;
